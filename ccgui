-- --- CONFIGURATION & STATE ---
local mobs = {"Zombie", "Skeleton", "Spider", "Creeper", "Enderman", "Blaze", "Witch"}
local state = {
    currentMenu = "main", -- "main" or "mobs"
    power = false,
    spawnerActive = false,
    selectedMob = "Select Spawner" -- Defaulted as requested
}

-- Get actual screen size for centering
local termWidth, termHeight = term.getSize()

-- --- DRAWING UTILITIES ---
local function clearScreen()
    term.setBackgroundColor(colors.black)
    term.clear()
end

local function drawRect(x, y, width, height, color)
    term.setBackgroundColor(color)
    for i = 0, height - 1 do
        term.setCursorPos(x, y + i)
        term.write(string.rep(" ", width))
    end
end

local function drawText(x, y, text, bg, fg)
    term.setBackgroundColor(bg)
    term.setTextColor(fg)
    term.setCursorPos(x, y)
    term.write(text)
end

-- --- DRAW TOGGLE SWITCH ---
local function drawToggle(x, y, isOn)
    drawRect(x, y, 5, 1, colors.gray)
    if isOn then
        drawRect(x + 3, y, 2, 1, colors.green)
        drawText(x + 1, y, "ON", colors.gray, colors.white)
    else
        drawRect(x, y, 2, 1, colors.red)
        drawText(x + 2, y, "OFF", colors.gray, colors.white)
    end
end

-- --- DYNAMIC CENTERING LAYOUT CALCULATIONS ---
-- Button width increased to 26 to comfortably fit " [ PUT SPAWNER AWAY ] "
local btnWidth = 26
local btnX = math.floor((termWidth - btnWidth) / 2) + 1

-- Width for label row centers
local contentWidth = 26 
local contentX = math.floor((termWidth - contentWidth) / 2) + 1

-- --- SCREEN RENDERERS ---
local function drawMainMenu()
    clearScreen()
    
    -- Centered Title Section
    drawRect(1, 1, termWidth, 3, colors.blue)
    local titleText = "SPAWNER GRID"
    local titleX = math.floor((termWidth - #titleText) / 2) + 1
    drawText(titleX, 2, titleText, colors.blue, colors.white)
    
    -- Power Section (Row 5)
    drawText(contentX, 5, "Power System:", colors.black, colors.white)
    drawToggle(contentX + 16, 5, state.power)
    
    -- Pull Spawner Button (Row 8) - Expanded & Centered
    drawRect(btnX, 8, btnWidth, 3, colors.lightGray)
    drawText(btnX + 4, 9, " [ PULL SPAWNER ] ", colors.lightGray, colors.black)
    
    -- Selected Mob Display (Row 12) - Centered text
    local activeText = "Active Mob: " .. state.selectedMob
    local activeX = math.floor((termWidth - #activeText) / 2) + 1
    drawText(activeX, 12, activeText, colors.black, colors.yellow)
    
    -- Spawner Active Switch (Row 14)
    drawText(contentX, 14, "Spawner Status:", colors.black, colors.white)
    drawToggle(contentX + 16, 14, state.spawnerActive)
    
    -- Put Spawner Away Button (Row 17) - Expanded & Centered
    drawRect(btnX, 17, btnWidth, 3, colors.lightGray)
    drawText(btnX + 2, 18, " [ PUT SPAWNER AWAY ] ", colors.lightGray, colors.black)
end

local function drawMobMenu()
    clearScreen()
    
    -- Centered Title
    drawRect(1, 1, termWidth, 3, colors.purple)
    local mobTitle = "SELECT A MONSTER"
    local mobTitleX = math.floor((termWidth - #mobTitle) / 2) + 1
    drawText(mobTitleX, 2, mobTitle, colors.purple, colors.white)
    
    -- List of Mobs - Centered boxes
    local listWidth = 22
    local listX = math.floor((termWidth - listWidth) / 2) + 1
    for i, mob in ipairs(mobs) do
        local yPos = 4 + i
        drawRect(listX, yPos, listWidth, 1, colors.gray)
        drawText(listX + 1, yPos, mob, colors.gray, colors.white)
    end
    
    -- Back Button - Placed neatly at the bottom center
    local backWidth = 10
    local backX = math.floor((termWidth - backWidth) / 2) + 1
    drawRect(backX, 17, backWidth, 3, colors.red)
    drawText(backX + 3, 18, "BACK", colors.red, colors.white)
end

local function render()
    if state.currentMenu == "main" then
        drawMainMenu()
    else
        drawMobMenu()
    end
end

-- --- INPUT HANDLING ---
local function handleMainClick(x, y)
    -- Power Toggle Click Detection
    if x >= (contentX + 16) and x <= (contentX + 20) and y == 5 then
        state.power = not state.power
        return true
    end
    
    -- Pull Spawner Button Click Detection
    if x >= btnX and x <= (btnX + btnWidth - 1) and y >= 8 and y <= 10 then
        state.currentMenu = "mobs"
        return true
    end
    
    -- Spawner Status Toggle Click Detection
    if x >= (contentX + 16) and x <= (contentX + 20) and y == 14 then
        state.spawnerActive = not state.spawnerActive
        return true
    end
    
    -- Put Spawner Away Button Click Detection
    if x >= btnX and x <= (btnX + btnWidth - 1) and y >= 17 and y <= 19 then
        state.selectedMob = "Select Spawner"
        state.spawnerActive = false
        return true
    end
    
    return false
end

local function handleMobClick(x, y)
    -- Back Button Click Detection
    local backWidth = 10
    local backX = math.floor((termWidth - backWidth) / 2) + 1
    if x >= backX and x <= (backX + backWidth - 1) and y >= 17 and y <= 19 then
        state.currentMenu = "main"
        return true
    end
    
    -- Mob Selection List Click Detection
    local listWidth = 22
    local listX = math.floor((termWidth - listWidth) / 2) + 1
    for i, mob in ipairs(mobs) do
        local yPos = 4 + i
        if x >= listX and x <= (listX + listWidth - 1) and y == yPos then
            state.selectedMob = mob
            state.currentMenu = "main"
            return true
        end
    end
    
    return false
end

-- --- MAIN PROGRAM LOOP ---
render()

while true do
    local event, side, x, y = os.pullEvent("mouse_click")
    
    local clicked = false
    if state.currentMenu == "main" then
        clicked = handleMainClick(x, y)
    elseif state.currentMenu == "mobs" then
        clicked = handleMobClick(x, y)
    end
    
    if clicked then
        render()
    end
end
